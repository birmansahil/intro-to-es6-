let name = 'Sahil';

// Both padStart and padEnd will add characters to make a String a specified length

// padStart adds at the front of String
console.log(name.padStart(10, 'a'));

// padEnd adds at the end of String
console.log(name.padEnd(10, 'a'));

